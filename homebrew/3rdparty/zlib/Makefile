all: build

# Download arguments
BASE_URL = http://zlib.net/

NAME = zlib-1.2.11

ARTIFACT = ${NAME}.tar.gz

# Build arguments
CONFIGURE_ARGS = --static

BUILD_TARGET = libz.a

BUILD_HEADERS = zlib.h zconf.h

INSTALL_TARGET = install

include ../Makefile.configure.base
